console.log("hello");
console.log(3+2);

// Ctrl + ~ will open the TERMINAL/COMMAND PROMPT/Console in VSCode

// To run a js file:
    // “node app.js”

// Shortcut for console.log()
    // Type ‘node’ in terminal (jumps INTO node)
    // now can type directly Into the terminal node
    // js can be executed without console.log
    // Type ‘.exit’ to finish

var importantEq = 45 * 2;
console.log("The important equation you need to know is: " + importantEq);

// Shift + alt + a
/* ljshglsg */

// ctrl + /

let num = 10;

if (num % 2 == 0) {
    console.log('This number is even.')
} else {
    console.log('This number is odd.')
}

function test(greeting) {
    console.log(greeting + " My name is Megan.")
}

var myGreeting = test("Bonjour");
console.log(myGreeting);


